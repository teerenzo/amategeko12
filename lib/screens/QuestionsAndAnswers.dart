import 'package:flutter/material.dart';

class Questions_and_answers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Questions_and_answers"),
      ),
      body: Center(
        child: Text("this is cquestion and answer page"),
      ),
    );
  }
}
