import 'package:flutter/material.dart';

class CarSigns extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("car signs"),
      ),
      body: Center(
        child: Text("this is car signs page"),
      ),
    );
  }
}
